"""GA4 client package — first-party attribution ground truth.

See `client.py` for the thin wrapper. Nothing re-exported here to keep
imports explicit — call sites do `from ads_agent.ga4.client import ga4_metrics`.
"""
